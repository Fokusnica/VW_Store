language: ruby
rvm:
  - 2.2.2
before_install:
    - gem update --system
    - gem --version
script:
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rake db:test:prepare
  - bundle exec rake
